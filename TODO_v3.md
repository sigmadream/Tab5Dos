## 목적

현재 TabDOS는 ESP32-P4 실기에서 FreeDOS와 `KOEI.COM`을 정상 실행하지만, 대항해시대2를 실제로 즐기기에는 CPU 처리량과 음악 동기화가 부족하다. 이 문서는 기존 명령 단위 인터프리터의 저위험 최적화를 넘어, 게임 실행 속도와 게스트 시간 기준 오디오 동기화를 함께 개선하기 위한 다음 작업 순서와 완료 기준을 정의한다.

## 현재 기준선

- 보드: M5Stack Tab5, ESP32-P4, CPU 360 MHz, PSRAM 32 MiB
- 펌웨어: ESP-IDF 6.0.1, C++20
- DOS 기준 처리량: 프로파일 빌드에서 약 0.59~0.74 MIPS
- KOEI 초기 핫스팟: `25A3:26F4~272B`
- 최적화 전 KOEI 안정 처리량: 약 0.10~0.11 MIPS
- 현재 프로파일 빌드 KOEI 처리량: 약 0.12~0.14 MIPS
- 현재 화면 갱신: 진행 구간에 따라 약 0.2~2.0 fps
- 호스트 KOEI형 산술 루프: 약 107 MIPS에서 약 119 MIPS로 개선
- 호스트 register-only 핫스팟 루프: 약 143 MIPS에서 약 203 MIPS로 개선
- 현재 보드에는 측정용 프로파일 펌웨어가 설치되어 있다. 프로파일러는 64개 명령마다 타이머를 읽고 5초마다 로그를 출력하므로 게임 성능의 최종 기준으로 사용하지 않는다.

## 최종 목표

- 동일한 KOEI 장면에서 프로파일러 없는 기본 빌드의 CPU 처리량을 현재 프로파일 기준 대비 최소 2배로 높인다.
- 화면이 실제로 변경되는 게임 구간에서 지속 5 fps 이상을 1차 목표로 한다.
- 게임 이벤트와 OPL/PC 스피커 출력의 누적 시간 차이를 5분 동안 100 ms 이하로 유지한다.
- 미지원 opcode, 인터럽트, 포트 접근 및 메모리 오류를 새로 만들지 않는다.
- DOS 프롬프트, 디스크 쓰기, USB 키보드, 마우스, VGA/Hercules/CGA 출력과 기존 오디오 기능을 유지한다.

## P0. 프로파일러 없는 실기 기준선 확정

- [ ] `TABDOS_CPU_PROFILE` 없이 기본 펌웨어를 빌드하고 플래시한다.
- [ ] DOS 프롬프트 도달 시간과 5개 이상의 CPU 처리량 구간을 기록한다.
- [ ] `KOEI.COM` 실행 후 동일한 초기 화면과 기존 `25A3:26F4~272B` 구간을 각각 30초 이상 측정한다.
- [ ] 세 번 실행한 처리량, 화면 fps, 평균 blit 시간을 기록하고 중앙값을 기준선으로 정한다.
- [ ] 프로파일 빌드와 기본 빌드의 오버헤드를 분리해 기록한다.
- 완료 기준: 재현 가능한 기본 빌드 기준선과 비교 절차가 문서에 남아 있다.

## P1. 게스트 시간과 오디오 시간의 관계 계측

- [ ] CPU 실행 단계, PIT 틱, OPL 레지스터 쓰기, PC 스피커 주파수 변경에 단조 증가 게스트 타임스탬프를 부여한다.
- [ ] 오디오 태스크의 실제 출력 샘플 수와 게스트 타임스탬프의 차이를 5초마다 기록한다.
- [ ] 음악이 앞서는지, 늦는지, 일시 정지 후 재개되는지 수치로 구분한다.
- [ ] 프로파일링은 기본 비활성화하고 별도 환경 변수로만 활성화한다.
- 완료 기준: 5분 실행에서 오디오 드리프트 방향과 최대값을 재현할 수 있다.

## P2. 오디오 동기화 정책 결정 및 구현

- [ ] 게스트 CPU가 실시간보다 느릴 때 사용할 정책을 비교한다.
  - 게스트 시간 우선: 오디오를 일시 정지하거나 샘플을 반복해 이벤트 순서를 보존한다.
  - 실제 시간 우선: 오디오를 계속 출력하되 오래된 OPL 이벤트를 병합한다.
- [ ] 대항해시대2에서는 게임 이벤트와 음악 전환이 맞는 게스트 시간 우선 정책을 기본 후보로 사용한다.
- [ ] OPL 레지스터 변경을 타임스탬프가 포함된 고정 크기 큐로 CPU 태스크에서 오디오 태스크로 전달한다.
- [ ] 오디오 태스크는 벽시계가 아니라 소비한 게스트 시간에 맞춰 레지스터 변경을 적용한다.
- [ ] 큐 초과, 언더런, 드롭 수를 진단할 수 있게 한다.
- 완료 기준: 5분 동안 게임 이벤트와 음악의 누적 시간 차이가 100 ms 이하이며 오디오 큐 오류가 없다.

## P3. Basic-block predecode 캐시

- [ ] CS:IP와 코드 바이트 세대를 키로 하는 고정 크기 basic-block 캐시를 설계한다.
- [ ] ModR/M, 변위, 즉시값, 피연산자 폭과 플래그 갱신 유형을 최초 실행 시 한 번만 해석한다.
- [ ] 분기, 인터럽트, 세그먼트 변경, 코드 메모리 쓰기에서 블록을 안전하게 종료하거나 무효화한다.
- [ ] 자체 수정 코드와 20비트 주소 래핑을 회귀 테스트로 고정한다.
- [ ] 동적 할당 없이 고정 크기 캐시와 명시적인 교체 정책을 사용한다.
- 완료 기준: KOEI형 호스트 벤치마크 20% 이상, ESP32-P4 동일 장면 15% 이상의 추가 향상과 전체 테스트 통과.

## P4. 메모리 접근 경로 추가 최적화

- [ ] 일반 RAM, EMS, 비디오 RAM, 레지스터 접근 비율을 구분해 계측한다.
- [ ] 일반 RAM의 읽기 전용 코드 페이지를 내부 RAM에 캐시했을 때의 효과를 측정한다.
- [ ] EMS 및 비디오 경계를 넘지 않는 일반 RAM 접근만 별도 빠른 경로로 유지한다.
- [ ] 1 MiB 래핑, 비정렬 워드, EMS 프레임 경계 테스트를 유지한다.
- 완료 기준: 정확성 테스트를 유지하면서 ESP32-P4 동일 장면에서 10% 이상의 추가 향상이 측정된다. 향상이 10% 미만이면 변경을 채택하지 않는다.

## P5. 프로파일 기반 명령 빠른 경로 확장

- [ ] 기본 빌드 기준으로 opcode별 누적 실행 시간과 CS:IP를 다시 수집한다.
- [ ] 전체 실행 시간의 5% 이상을 차지하는 형식만 후보로 선정한다.
- [ ] register-only 형식과 일반 메모리 형식을 분리하고, 빠른 경로마다 값과 FLAGS 회귀 테스트를 먼저 추가한다.
- [ ] 중복 구현으로 얻는 실기 향상이 3% 미만이면 빠른 경로를 제거한다.
- 완료 기준: 작은 diff, 명확한 측정값, 동작 고정 테스트가 모두 존재한다.

## P6. 디스플레이와 태스크 스케줄링 검토

- [ ] CPU 태스크의 mutex 대기, `taskYIELD`, 1 tick 휴식, 오디오 태스크와의 코어 경쟁 시간을 계측한다.
- [ ] 화면이 변하지 않는 구간에서 렌더링 및 프레임 비교 비용이 CPU 처리량에 영향을 주지 않는지 확인한다.
- [ ] 화면 변경 구간에서는 PPA 제출과 CPU 에뮬레이션이 가능한 한 겹치도록 유지한다.
- [ ] CPU 슬라이스 크기는 키보드와 터치 지연 20 ms 이하를 유지하는 범위에서 조정한다.
- 완료 기준: 입력 지연 회귀 없이 CPU 태스크의 비실행 시간이 감소하고 실기 처리량이 향상된다.

## P7. 최종 게임 검증

- [ ] 프로파일러 없는 기본 펌웨어로 콜드 부팅부터 `KOEI.COM` 실행까지 세 번 반복한다.
- [ ] 타이틀, 메뉴, 항해, 전투 또는 장시간 음악 구간을 각각 5분 이상 실행한다.
- [ ] CPU MIPS, 화면 fps, 오디오 드리프트, 디스크 오류, 미지원 기능 카운터를 기록한다.
- [ ] 재부팅 후 저장 파일 읽기와 쓰기를 확인한다.
- [ ] 프로파일 기능을 다시 활성화해도 기본 빌드 동작이 바뀌지 않는지 확인한다.
- 완료 기준: 목표 처리량과 오디오 동기화 기준을 만족하고 새 오류가 없다.

## 우선순위와 중단 기준

1. P0으로 실제 기본 빌드 기준선을 먼저 확정한다.
2. P1과 P2로 속도와 별개인 음악 싱크 문제를 수치화하고 수정한다.
3. P3 basic-block predecode를 다음 주요 CPU 성능 작업으로 진행한다.
4. P4~P6은 각 단계의 실기 개선값이 완료 기준을 만족할 때만 채택한다.
5. 동적 재컴파일/JIT는 basic-block 캐시로 목표를 달성하지 못한 뒤에만 별도 설계한다.
6. 정확성 회귀, 저장 데이터 손상, 입력 지연 증가가 발생하면 해당 최적화를 중단하고 되돌린다.

## 검증 명령

호스트 Release 테스트:

```sh
cmake -S tests -B .test-build -DCMAKE_BUILD_TYPE=Release
cmake --build .test-build -j4
ctest --test-dir .test-build --output-on-failure
```

기본 ESP32-P4 빌드:

```sh
idf-venv
unset TABDOS_CPU_PROFILE
idf.py reconfigure build flash monitor
```

프로파일 빌드:

```sh
idf-venv
TABDOS_CPU_PROFILE=1 idf.py reconfigure build flash monitor
```
