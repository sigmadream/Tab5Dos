# Task_20260710 — 남은 작업 · 실기 검증 준비 · 신규 도구

`Task_20260707.md`의 개선 작업(T1~T12) 중 **코드 작업은 모두 완료(✅)**, 실기 하드웨어가
필요한 조사 작업(T8)만 미착수 상태다. 이 문서는 남은 작업과, 2026-07-09~10에 추가한
지원 도구(스크린샷 캡쳐), 그리고 그 과정에서 관찰된 후속 조사 항목을 정리한다.

목표: 실기 검증(대항해시대2 기동, 스크린샷 파이프라인)을 수행할 수 있는 상태로 마무리한다.

---

## 이월 요약 (2026-07-07 기준)

| Task | 상태 | 비고 |
|------|------|------|
| T1~T7, T9~T12 | ✅ 코드 완료 | 상세·설계 노트는 `Task_20260707.md` 참조 |
| T8 실기 기동 테스트 | ⏸ 하드웨어 필요 | 아래 **R1**로 승계 |

포터블 컴포넌트(`components/tabdos_pc`)는 호스트 g++ `-fsyntax-only` 통과. `main/`은
ESP-IDF 전용 헤더 의존이라 실기 빌드(`idf.py build`)로만 검증 가능.

---

## 신규 지원 도구 (2026-07-09~10, 완료)

### S1. 화면 스크린샷 캡쳐 (시리얼 → 이미지) ✅

실기 화면을 호스트에서 이미지로 확보하기 위한 기능. **R1(실기 테스트)의 진단·문서화와
버그 리포트 첨부를 직접 지원**한다.

- 트리거: 패널 **우상단 코너(120×120px) 롱프레스 1.5초** (`main/main.cpp` touchMouseTask).
  제스처 중 터치는 에뮬 마우스로 전달하지 않아 오클릭 없음.
- 캡쳐 대상: 회전 전 **원본 PC 소스 프레임**(텍스트 720×400 / 그래픽 640×480 등, RGB565).
- 전송: 프레임을 **RLE 압축(`{개수 u16, 색 u16}`) 후 base64**로 시리얼 출력. 각 줄 `SS:` 프리픽스,
  `BEGIN/END` 마커로 감쌈. 노이즈가 많아 압축이 원본보다 커지면 무압축으로 자동 폴백.
  - 텍스트 프레임 실측: 576KB → 51KB(**~11×**), 10,667줄 → 952줄. **~6s @115200 / ~0.7s @921600.**
- 호스트: `tools/decode_screenshot.py` — 파일 내 **다중 덤프를 모두** BMP로 복원(`_000`, `_001`…),
  잘리거나 손상된 덤프는 건너뜀. 무압축·RLE 포맷 자동 분기. `--index N`으로 단일 지정 가능.
- 변경 파일: `main/tab5_lcd_text.*`(`dumpSourceFrameBase64`, `drawFrame` 소스 추적),
  `main/main.cpp`(`screenshotRequested` 플래그, 코너 롱프레스, displayTask 훅),
  `tools/decode_screenshot.py`(신규).
- 검증: 실캡쳐 프레임으로 RLE 인코딩→디코딩 라운드트립이 무압축 복원과 **바이트 단위 동일**함을
  호스트에서 확인. 다중 덤프/혼합 포맷/미완성 덤프 제외/인덱스 범위초과도 통과.
- 2026-07-12 갱신: ESP-IDF 6.0.1에서 펌웨어 빌드와 ESP32-P4 플래시를 완료했고,
  사용자가 실제 보드의 정상 동작을 확인했다. 스크린샷 블록의 신규 수집·복원 증거는 아래 R2에서 계속 추적한다.

---

## 남은 작업

### R1. 대항해시대2 실기 기동 테스트 (구 T8, ⏸ 하드웨어 필요)

T4(CPU 검출 opt-in)·T7(EMS)의 실제 필요 여부를 판정하는 조사. **코드 변경 없음.**
Tab5 실기 + 대항해시대2 이미지 필요. S1 스크린샷 도구를 화면 기록에 활용한다.

- [x] `idf.py set-target esp32p4 && idf.py build flash monitor`로 플래시
  - 2026-07-12 `tabdos_tab5.bin` 774,688바이트 생성, ESP32-P4 플래시 쓰기와 해시 검증 완료
- [ ] 대항해시대2 HDD 이미지를 `/sdcard/dos/hd0.img` 등으로 배치 후 부팅하고 `KOEI.COM` 실행
  - 사용자 확인: 대항해시대2의 진입점은 `KOEI.COM`이다.
  - 정적 확인: `KOEI.COM`은 `FMDRV.COM`, `OPEN.EXE`, `MAIN.EXE`, `END.EXE`를 참조하는 런처이며,
    EMS 함수 58h를 사용하는 세 EXE로 실행 흐름을 연결한다.
- [ ] 시리얼 로그 관찰: `CPU throughput: N MIPS`, `display: N fps drawn`(T2),
      `OPCODE milestone: unsupported op=..`(T9), `diagnostic int=.. portR/W=..`, `PC speaker audio enabled`(T5)
  - 2026-07-12 DOS 기준선: CPU 0.52~0.84 MIPS, 활성 화면 0.8~5.0 fps, 평균 blit 약 52~53ms,
    DOS 프롬프트와 DIR 목록 검출, USB 키보드 입력 및 PC 스피커 활성화를 확인했다.
  - 추가 로그에서는 약 28초 이후 CPU가 0.80 MIPS에서 0.13~0.17 MIPS로 떨어지고 화면 갱신이
    0 fps가 됐다. 이 구간이 `KOEI.COM` 실행 이후라면 초기화 대기 또는 루프 정체 가능성이 있으나,
    실행 명령과 화면 증거가 없어 아직 판정하지 않는다.
  - 저속 상태는 약 95초간 이어졌지만 123초 부근에 11프레임이 갱신되어 완전한 CPU 정지는 아니다.
    새 EMS 빌드에서 같은 구간의 시간과 화면 변화를 비교해야 한다.
  - 2026-07-12 최종 opcode 수정 펌웨어에서 `KOEI.COM`을 다시 실행해 실시간 측정했다. DOS 프롬프트는
    0.68~0.83 MIPS였지만 KOEI 진입 후 0.29 -> 0.15 -> 0.11~0.12 MIPS로 떨어졌고, 초기 1.6 fps 뒤
    화면 변경이 장시간 0 fps로 유지됐다. 미지원 opcode/인터럽트 및 디스크 오류는 새로 기록되지 않았다.
    LCD blit이 없는 구간에도 CPU 처리량이 낮으므로 디스플레이 하드웨어 병목보다는 인터프리터의 특정
    명령군, EMS/메모리 접근, REP 내부 반복 또는 게스트 대기 루프를 우선 프로파일링해야 한다.
  - `OPCODE milestone`은 관찰되지 않았으나 `lastInt=66 ah=f1`인 미지원 인터럽트 1건이 기록되어 R7로 추가했다.
- [ ] **T4 판정**: "286 이상 필요" 없이 기동하면 기본(8086 보고, F2 이후) 유효. 286을 강제 검사하고
      0F 명령을 안 쓰면 부팅 시 `PcI8086::setReportAbove8086(true)` 호출 지점을 `app_main`에 추가
  - 2026-07-12 `OPEN.EXE`, `MAIN.EXE`, `END.EXE`, `GRPDRV.EXE`, `FMDRV.COM`, `KOEI.COM`에서
    일반적인 PUSHF/POPF 기반 8086·286 판별 패턴과 SMSW 검사 패턴을 검색했으나 발견되지 않았다.
    정적 근거상 286 보고를 강제할 이유는 없으며, 기본 8086 보고 유지가 우선이다. 최종 판정은 실기 기동으로 확인한다.
- [ ] **T7 판정**: 게임이 EMS 요구/사용 시 `int=67` 미지원 로그가 없어야 정상. FreeDOS `MEM /E`로 4MB EMS 인식 확인
  - 2026-07-12 정적 분석에서 `OPEN.EXE`, `MAIN.EXE`, `END.EXE`가 모두 `EMMXXXX0`을 포함하고,
    EMS 초기화 루틴이 INT 67h의 40h, 41h, 42h, 58h 함수를 실제 호출하는 것을 확인했다.
  - 기존 구현은 필수 AX=5801h 호출에 AH=84h를 반환해 게임의 EMS 초기화를 중단시켰다.
  - LIM EMS 4.0 함수 58h의 주소 배열 조회와 항목 수 조회를 구현하고, EMM 버전 보고를 4.0으로 갱신했다.
    4개 물리 페이지의 `E000h/E400h/E800h/EC00h` 배열과 잘못된 하위 함수 오류를 호스트 회귀 테스트로 검증했다.
  - 새 펌웨어는 부팅 시 `EMS: LIM 4.0, 4096 KiB, page frame e000`을 출력하므로 적용 여부를 로그에서 확인할 수 있다.
  - 사용자가 제공한 실기 로그의 앱 컴파일 시각은 14:44:11이고 함수 58h 수정 시각은 14:58 이후이므로,
    해당 로그는 새 EMS 구현의 실기 증거가 아니다. 새 빌드·플래시 후 다시 판정해야 한다.
- [ ] 마우스 커서(T1)·화면 부드러움(T2)·사운드(T5/T11) 체감을 S1 스크린샷과 함께 기록
- 완료 기준: T4/T7 필요 여부가 판정되고, 발견된 미지원 opcode/인터럽트/포트가 후속 태스크로 추가된다.

### R2. 스크린샷 기능(S1) 실기 빌드·동작 검증

S1은 호스트에서 알고리즘만 검증됐다. 실기 빌드/플래시 후 실제 캡쳐를 확인한다.

- [x] `idf.py build flash monitor`로 컴파일 오류 없이 빌드되는지 확인 (신규 코드 3파일)
  - 2026-07-12 ESP-IDF 6.0.1 빌드 성공, 앱 파티션 26% 여유, 플래시 해시 검증 완료
  - 사용자가 실제 보드에서 정상 동작을 확인함
- [ ] 코너 롱프레스 → `=== TABDOS SCREENSHOT BEGIN … END ===` 블록이 온전히 출력되는지 확인
- [ ] `idf.py monitor | tee dump.txt` 저장 후 `python3 tools/decode_screenshot.py dump.txt shot.bmp`로 복원 확인
- [ ] (선택) 전송이 느리면 콘솔 baud 상향(`CONFIG_ESP_CONSOLE_UART_BAUDRATE=921600`) 후 `idf.py -b 921600 monitor`
- 완료 기준: 실기에서 720×400 프레임이 잘림 없이 이미지로 복원된다.

### R3. QBasic 실행 중 관찰된 "Invalid Opcode" 조사 (관찰 기반)

2026-07-10 S1으로 캡쳐한 QBasic 편집기 화면 상단에 `Invalid Opcode at 7E37 610C FA92 0004 …`
표시가 관찰됨. 실제 미지원 명령인지, 프로그램/런타임의 자체 메시지인지 확인 필요.

2026-07-12 호스트 선행 분석:

- 커밋된 720×400 캡처 4개를 다시 확인했으나 해당 문구는 남아 있지 않다. 캡처에는 QBasic 편집기,
  QBasic 실행 결과, GW-BASIC, IBM ART 화면이 기록되어 있다.
- 에뮬레이터의 미지원 opcode 경로는 `Unsupported 8086 opcode ...`와 `OPCODE milestone ...`을
  시리얼 및 `tabdos.log`에만 기록하며 화면 버퍼에는 문구를 쓰지 않는다.
- 따라서 화면의 `Invalid Opcode` 문구가 재현되면 게스트의 예외 처리기 또는 BASIC 런타임이 출력한
  것으로 판정할 수 있다. 같은 시점의 화면 캡처와 `OPCODE milestone` 로그를 함께 확보해야 원인 명령과
  명령 스트림 desync 여부를 연결할 수 있다.
- 이후 비정렬 워드 접근 수정 빌드의 실기 부팅 초기에 `F1 00 at 0000:0021`과
  `F1 00 at 0000:0071`이 재현됐다. 두 주소는 프로그램 코드가 아닌 인터럽트 벡터 테이블 내부이므로
  F1 구현 누락이 아니라 제어 흐름 또는 스택 손상으로 판정했다.
- 비정렬 워드 프록시끼리 대입할 때 값이 아니라 프록시 포인터가 복사되어 80186 `ENTER`의 중첩 프레임
  복사가 누락되는 별도 회귀를 발견해 수정했고 `ENTER 0,2` 테스트를 추가했다. 그러나 수정 펌웨어에서도
  동일한 두 F1 위치가 재현되어 이것이 F1 현상의 근본 원인이라는 가설은 기각했다.
- 같은 HDD 이미지를 호스트에서 약 0.6 MIPS로 8초간 부팅했을 때는 약 368만 단계 동안 미지원 opcode가
  재현되지 않았고, 실기 LCD는 `FreeDOS123`까지 정상 출력됐다. 다음 실기 로그에서 저메모리 진입 직전
  CS:IP/opcode와 진입 대상, SS:SP를 함께 출력하도록 제어 흐름 진단을 확장했다.
- 확장 로그로 `9001:00F4`의 `IRET`가 `SS:SP=9DCA:2330` 부근의 프레임을 복원한 뒤
  `0000:0000`으로 진입하는 것이 확인됐다. 이후 IVT를 순차 실행하며 `0000:0021`과 `0000:0071`의
  벡터 오프셋 상위 바이트 `F1`을 opcode로 만난다. 따라서 F1은 원인이 아니라 잘못된 IRET의 결과다.
- F1 시점에 뒤늦게 읽은 `9DCA:2330` 프레임은 첫 로그와 두 번째 로그의 값이 서로 달랐다. IVT를 코드로
  실행하는 동안 이미 같은 메모리가 덮어써졌으므로 해당 값은 원래 IRET 프레임의 증거로 사용할 수 없다.
- 진단을 IRET 실행 직전으로 옮겨 정확한 IP/CS/FLAGS, IRET 주변 32바이트, 같은 코드 세그먼트를 가리키는
  모든 IVT 벡터를 출력하도록 수정했다. 이를 통해 프레임 미생성 호출인지 특정 IRQ/소프트웨어
  인터럽트의 스택 손상인지 판정한다.
- 최신 실기 로그에는 `Bad IRET`가 출력되지 않았지만 다음 명령 단계에서 저메모리 진입이 기록됐다.
  로컬 ELF/BIN에 해당 진단 문자열이 포함된 것도 확인했으므로 IRET는 정상 주소를 복원했고, 그 직후
  CPU 단계 시작 시 처리된 인터럽트가 `0000:0000` 벡터로 진입했을 가능성이 높다. IRET가 TF를
  복원한 뒤 null INT 01h 벡터로 진입하는 경로가 우선 가설이다.
- `pc_interrupt`가 처리되지 않은 null 벡터를 사용하기 직전에 인터럽트 번호, 원래 CS:IP, SS:SP,
  FLAGS를 출력하도록 진단을 추가했다. 다음 실기 로그의 `Null interrupt vector INT ..`로 확정한다.
- 실기에서 `Null interrupt vector INT 01 from FFFE:9DCA ... flags=FFD6`가 확인되어 원인을 확정했다.
  FreeDOS의 TF 기반 탐지 뒤 INT 01h 기본 벡터가 비어 있어 `0000:0000`으로 진입했고, 이후 IVT의
  F1 바이트를 opcode로 실행했다. 또한 `FFFE:9DCA`는 1MiB를 넘는 선형 주소이므로 8086의 20비트
  주소 버스에서는 물리 `09DAA`로 래핑되어야 한다.
- 게스트 INT 01h 벡터가 없을 때 BIOS가 one-shot trap을 소비하고 TF를 해제하도록 했으며, 모든 일반
  RAM 접근과 명령어 fetch에 20비트 주소 래핑을 적용했다. 1MiB 경계 워드 읽기/쓰기, 상위 세그먼트
  명령 fetch, TF+IRET+null INT 01h 시나리오를 회귀 테스트로 추가했다.
- 원인 확정 후 매 명령마다 실행되던 저메모리 추적은 제거했다. Release 및 ASan/UBSan 전체 6개 테스트와
  동일 HDD 이미지의 약 0.6 MIPS 호스트 부팅 369만 단계에서 미지원 opcode 0건을 확인했다.
- 실제 보드 후속 검증에서 INT 01h를 안전하게 처리한 뒤에도 `FFFE:*`, `9DCA:*`의 데이터 영역을
  명령으로 실행하는 문제가 남았다. 제어 흐름 추적으로 `FFFE:3837`의 `FF E0`(`JMP AX`)에서
  `AX=3848`, 원시 레지스터 바이트 `48 38`인데 실제 점프 대상이 `0048`로 잘리는 것을 확인했다.
- 근본 원인은 비정렬 워드 프록시와 `uint8_t`를 삼항 연산자에서 혼합하여 C++ 공통 타입이
  `uint8_t`로 결정된 것이었다. `MEM16` 결과를 명시적으로 `uint16_t`로 변환해 간접 near JMP/CALL의
  상위 바이트를 보존하고, `JMP AX`가 `7C10`으로 이동하는 회귀 테스트를 추가했다.
- 임시 IRET/INT01/저메모리/FF E0 덤프를 제거한 최종 펌웨어를 `idf-venv`와
  `idf.py flash monitor`로 직접 빌드·플래시했다. 앱 바이너리 777,968바이트, 플래시 해시 검증 성공,
  미지원 opcode 0건, 11.0초에 DOS 프롬프트 검출, CPU 0.68~0.83 MIPS를 실기에서 확인했다.

- [ ] 해당 상황 재현 후 화면 캡처와 T9 진단 경로(`UnsupportedOpcode` 콜백)의 CS:IP·opcode 바이트를 동시 수집
- [ ] 원인 명령 식별 → 구현/스킵/무시 판정. 0F 계열(286+)이면 T4 opt-in과의 상호작용 검토(F2 참조)
- [ ] 명령 스트림 desync(T9의 0x63-0x65류) 가능성 배제
- 완료 기준: 표시된 opcode의 정체가 확인되고 대응(구현 또는 정상 판정)이 결정된다.

---

## 선택 / 후속 (필요 시 착수)

### R4. 텍스트 모드 스크린샷 초고속화

텍스트 모드는 렌더 픽셀 대신 **80×25×2 = 4KB 문자·속성 버퍼**만 전송하고 호스트에서
8×16 폰트로 렌더링하면 ~0.5s로 단축(144×↓). 단, 호스트 디코더가 폰트·팔레트·9-dot 렌더링을
재현해야 하며 텍스트 모드에만 적용. **R2에서 전송 속도가 병목일 때만 착수.**

### R5. 스크린샷 SD 카드 저장 옵션

시리얼 대기 없이 기기에서 `/sdcard/dos/`에 BMP로 직접 저장(즉시 캡쳐, 카드만 빼서 열람).
기존 `Tab5RuntimeLog`의 `fopen/fwrite/fsync` 패턴 재사용. 그래픽 모드 등 대용량 프레임에 특히 유리.

### R6. Mode X / 추가 게임 실기 확인

T12(Mode X)는 코드상 완비되어 있으나 실기 검증 미수행. Mode X를 쓰는 게임(1992~ 액션류)
확보 시 더블버퍼 스크롤 동작을 실기에서 확인.

### R7. INT 66h AH=F1 미지원 호출 조사

2026-07-12 DOS 부팅 후 키보드 입력과 DIR 실행 과정에서
`diagnostic int=1 portR=0 portW=0 lastInt=66 ah=f1`가 한 번 기록됐다.

- [ ] 동일 명령 흐름에서 재현하고 직전 실행 프로그램을 기록
- [ ] 확장된 진단 로그의 `lastInt=66 ah=f1 at CS:IP`로 호출 지점을 수집
- [ ] INT 66h 벡터의 소유자와 AH=F1 계약을 식별
- [ ] 애플리케이션 자체 벡터 설치 누락인지 BIOS/드라이버 호환성 누락인지 판정
- 완료 기준: 호출 주체와 계약을 확인하고 구현 또는 정상 무시 여부를 결정한다.
## KOEI CS:IP 및 opcode 실행 시간 프로파일링

- `TABDOS_CPU_PROFILE=1` 환경 변수로만 활성화되는 샘플링 프로파일러를 추가했다.
- 모든 opcode의 실행 횟수를 집계하고 64개 명령마다 1개를 `esp_timer_get_time()`으로 측정한다.
- 5초마다 샘플 실행 시간 기준 상위 opcode 8개와 CS:IP 기준 상위 핫스팟 8개를 출력한다.
- 프로파일링 펌웨어는 ESP32-P4 보드에 직접 플래시했고, 플래시 데이터 해시 검증과 DOS 부팅 및 KOEI 실행을 확인했다.
- 프로파일링 전 DOS 프롬프트 구간은 약 0.69~0.70 MIPS였고, KOEI 진입 후 약 0.10~0.13 MIPS로 내려갔다.
- KOEI의 초기 대기 루프에서는 `25A3:46A0~46A7` 및 `2461:013E~0183`가 반복됐다. 이후 안정 구간의 주 핫스팟은 `25A3:26F4~272B`였다.
- 안정 구간에서 반복적으로 관측된 opcode 비율은 `FE` 약 7.7%, `02` 약 7.6%, `79` 약 7.6%, `13` 약 6.9%, `FF` 약 4.4~5.2%, `8A/8B` 각각 약 4.4~4.7%였다.
- 대표적인 핫스팟은 `25A3:26F4 FE`, `25A3:2708 41`, `25A3:2709 02`, `25A3:270D 13`, `25A3:2713 FE`, `25A3:2715 79`, `25A3:2727 02`, `25A3:2729 13`, `25A3:272B E2`였다.
- 각 샘플의 일반 명령 실행 시간은 대체로 3~6 us였고 특정 단일 opcode가 압도적인 실행 시간을 차지하지 않았다. 따라서 현재 저속 현상은 한 개의 비정상적으로 느린 명령 구현보다는 산술, 메모리 접근, 조건 분기, `LOOP`로 구성된 게스트의 조밀한 반복 루프를 인터프리터가 실행하는 비용에 가깝다.
- 프로파일링 자체의 영향을 피하기 위해 기본 빌드에서는 비활성화하며, 다음 명령으로 활성화한다.

```sh
TABDOS_CPU_PROFILE=1 idf.py reconfigure build flash monitor
```

## KOEI 인터프리터 핫패스 최적화

- 프로파일에서 반복된 `25A3:26F4~272B` 구간을 기준으로 일반 RAM과 레지스터 접근 경로를 분리했다.
- 레지스터 피연산자는 비디오 RAM 및 EMS 범위 판정을 거치지 않고 즉시 레지스터 배열에 접근한다.
- 일반 RAM 워드 접근은 1MiB 경계 래핑일 때만 안전 프록시를 사용하고, 나머지는 직접 바이트 읽기/쓰기를 사용한다.
- `LOOPNZ`, `LOOPZ`, `LOOP`와 `INC/DEC reg16`을 공통 ModR/M 디코더에서 분리했다.
- 핫스팟의 2바이트 명령인 `ADD r8,r8`, `ADC r16,r16`, `INC/DEC r8`의 register-only 형식만 빠른 경로로 처리한다. 메모리 피연산자 형식은 기존 공통 디코더를 유지한다.
- `LOOP` 계열, `INC/DEC reg16`, register-only `ADD/ADC/INC/DEC`의 결과와 CF/AF/OF/SF/ZF/PF를 회귀 테스트로 고정했다.
- KOEI형 일반 산술 루프의 호스트 중앙값은 약 107 MIPS에서 약 119 MIPS로 약 11% 증가했다.
- register-only 핫스팟 재현 루프는 약 143 MIPS에서 약 203 MIPS로 약 42% 증가했다.
- 첫 번째 최적화 펌웨어의 KOEI 실기 안정 구간은 기존 약 0.10~0.11 MIPS에서 약 0.12 MIPS로 증가했다.
- 마지막 register-only 최적화 펌웨어에서 사용자가 실행 중인 KOEI에 리셋 없이 직접 시리얼로 연결했다. 기존 `25A3:26F4~272B` 루프는 0.12~0.14 MIPS로 실행됐고, 이후 다른 KOEI 코드 구간으로 진행하면서 0.13~0.14 MIPS와 0.2~2.0 fps의 실제 화면 갱신이 관찰됐다. 최적화 전 장시간 0.10~0.11 MIPS 및 0 fps였던 구간과 비교하면 CPU 처리량은 대략 20~30% 증가했고 정체 구간을 벗어나 진행하는 것도 확인됐다.
- 프로파일 펌웨어 780,304바이트와 기본 펌웨어 778,448바이트가 ESP-IDF 6.0.1에서 빌드됐다. 기본 앱 파티션은 26% 여유가 있다.
- Release 및 ASan/UBSan 테스트는 각각 6개 모두 통과했다.
