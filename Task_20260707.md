# Task_20260707 — 대항해시대2 · QBasic · GW-BASIC 구동 개선 작업

2026-07-07 코드 리뷰 결과를 기반으로 한 개선 작업 목록.
목표: 대항해시대2(KOEI, 1993)를 원활히 플레이하고, QBasic/GW-BASIC을 실용 수준으로 구동한다.

리뷰 요약: CPU 코어(80186 명령어 풀셋)와 BIOS 인터럽트(INT 10h/13h/16h/33h)는 충분히 탄탄함.
공백은 ① PIT/사운드 계열 부재, ② 마우스 커서 미표시, ③ 화면 갱신(10fps)·수직귀선 타이밍 부족에 집중.

---

## P1 — 구동/조작에 직결 (착수 순서대로)

### T1. 마우스 커서 렌더링 ✅

INT 33h 상태는 완전하지만 커서가 화면에 그려지지 않는다. QBasic IDE와 대항해시대2 UI 필수 요소.

- 대상: `components/tabdos_pc/pc/pc_bios.cpp:2226-2351` (INT 33h), `pc_machine.cpp` 렌더 경로, `main/main.cpp` displayTask
- 작업:
  - [x] INT 33h AX=0001/0002의 표시 카운터를 실제 렌더링에 연결 (카운터 >= 0일 때만 표시)
  - [x] 텍스트 모드: 커서 위치 셀의 속성 반전으로 표시
  - [x] 그래픽 모드: 기본 화살표 스프라이트를 프레임 합성 단계(`renderGraphicsFrameForDisplay`)에서 오버레이
  - [x] AX=0009(사용자 정의 커서 모양) 실제 반영 — 현재 no-op 스텁
  - [x] AX=0010(exclusion area) 처리 — 게임이 화면 갱신 영역에서 커서 깜빡임 방지에 사용
- 완료 기준: QBasic IDE에서 마우스 포인터가 보이고 메뉴 클릭이 가능하다. 터치→마우스 경로에서도 동일.

### T2. 화면 갱신 주기 단축 (10fps → 30fps 이상) ✅

`DisplayDelay = 100ms`(main.cpp:29)는 텍스트용 설정. 게임 애니메이션에는 부족.

- 대상: `main/main.cpp` displayTask, `main/tab5_lcd_text.cpp`, `pc_machine.cpp` 렌더러
- 작업:
  - [x] DisplayDelay를 33ms 이하로 단축하고 CPU 태스크와의 뮤텍스 경합 측정 (cpuTask는 200,000 명령 루프 동안 뮤텍스 보유 — 슬라이스 분할 필요할 수 있음)
  - [x] 더티 라인 추적 또는 프레임 비교로 변경된 라인만 blit — 모드 13h(320×200)는 픽셀 수가 적어 효과 큼
  - [x] 회전·스케일 경로(720×1152) 병목이면 ESP32-P4 PPA(Pixel Processing Accelerator)/DMA2D 하드웨어 가속 검토
  - [x] 갱신 주기 단축 후 실측 fps와 CPU 에뮬레이션 처리량(MIPS)을 시리얼 로그로 출력하는 진단 추가
- 완료 기준: 모드 13h에서 30fps 이상 유지하면서 에뮬레이션 처리량이 기존 대비 20% 이상 저하되지 않는다.

### T3. PIT(8253) 재구현 ✅

현재 채널 0 읽기는 고정 감산(`m_pitCounter -= 0x0137`, pc_machine.cpp:990-999)이고,
모드 레지스터(0x43)와 리로드 값 쓰기는 전부 무시된다. 음악 드라이버·정밀 타이밍 코드의 전제 조건.

- 대상: `components/tabdos_pc/pc/pc_machine.cpp` readPort/writePort(0x40-0x43), 타이머 IRQ 디스패치(pc_machine.cpp:821-829)
- 작업:
  - [x] 채널 0/2 리로드 카운트 저장 및 latch/read-back 명령 처리 (0x43 모드 바이트 해석: 채널 선택, 접근 모드 lobyte/hibyte/both, 동작 모드 2/3)
  - [x] 채널 0 리로드 값에 따라 IRQ0 발화 주기 반영 — 현재 벽시계 18.2Hz 고정. 재프로그래밍 시 비율 환산하여 BIOS 틱(0040:006C)은 18.2Hz 유지, 사용자 INT 08h 후킹에는 재프로그래밍된 주기 제공
  - [x] 채널 2 카운트다운 구현 (T5 PC 스피커의 선행 조건)
  - [x] 카운터 읽기를 실제 경과 시간 기반 위상으로 계산 (고정 감산 제거)
- 완료 기준: PIT를 재프로그래밍하는 테스트 프로그램(예: 100Hz INT 08h 후킹)이 의도한 주기로 인터럽트를 받는다. 기존 DOS 부팅·시계는 깨지지 않는다.

### T4. CPU 타입 검출 대응 (286 판별) ✅

CPU가 자신을 8086으로 보고(`I80186MODE=0`)하므로, PUSHF의 FLAGS 비트 12-15 패턴으로
CPU 세대를 판별하는 게임(대항해시대2 포함 가능)이 "286 이상 필요"로 실행 거부할 수 있다.

- 대상: `components/tabdos_pc/pc/pc_i8086.cpp` (PUSHF/POPF, FLAGS 처리)
- 작업:
  - [x] 실기에서 대항해시대2 기동 테스트로 CPU 검사 여부 먼저 확인 (T8과 함께 진행)
  - [x] 필요 시: PUSHF에서 FLAGS 비트 12-14(IOPL/NT)를 0으로 보고하는 "286처럼 보이기" 옵션 추가 (컴파일 플래그 또는 런타임 설정)
  - [x] 8086 전제 소프트웨어 회귀 확인 (기존 FreeDOS 부팅, GW-BASIC)
- 완료 기준: 대항해시대2가 CPU 검사를 통과하고 기존 이미지 부팅에 회귀가 없다.

---

## P2 — 완성도 (사운드·타이밍·메모리)

### T5. PC 스피커 출력 (사운드 1단계) ✅

포트 61h는 저장만 하고 사용하지 않음(pc_machine.cpp:1007,1133). Tab5의 I2S 코덱/스피커로 구형파 출력.
GW-BASIC/QBasic의 BEEP·SOUND·PLAY, 대항해시대2 스피커 모드가 살아난다. **T3 선행 필요.**

- 대상: `pc_machine.cpp` (61h + PIT ch2), `main/` 신규 오디오 모듈 (BSP I2S/코덱 경로)
- 작업:
  - [x] 61h 비트 0(PIT ch2 게이트)·비트 1(스피커 데이터)과 ch2 리로드 값 → 주파수 계산
  - [x] main/에 I2S 출력 태스크 신설 (구형파 생성, 주파수 변경 시 클릭 노이즈 방지 램프)
  - [x] 에뮬레이터 코어는 하드웨어 독립 유지 — 콜백/폴링 인터페이스로 main/에 전달 (tabdos_pc에 ESP 의존성 넣지 않기)
- 완료 기준: GW-BASIC에서 `PLAY "CDEFGAB"`가 음계로 들린다.

### T6. 수직귀선(3DAh) 타이밍 일관성 ✅

상태 비트가 합성 위상(pc_machine.cpp:149-159)이라 귀선 폴링 기반 팔레트 페이드/더블버퍼 코드가
가짜 귀선을 볼 수 있다.

- 대상: `pc_machine.cpp` vgaInputStatus 관련 (3BAh/3DAh)
- 작업:
  - [x] 초당 60프레임 가정의 프레임 주기에 동기화된 귀선 시뮬레이션 (수직귀선 구간 비율 반영, 수평귀선도 위상 기반)
  - [x] 귀선 대기 루프(비트 3 폴링)가 실제 시간 기준으로 블로킹/해제되는지 테스트 프로그램으로 검증
- 완료 기준: 귀선 동기 팔레트 페이드가 찢어짐 없이 일정한 속도로 동작한다.

### T7. EMS(INT 67h) 소프트웨어 구현 ✅

현재 완전 부재. 대항해시대2 한글판·사운드 드라이버가 EMS를 요구할 경우의 대비책.
PC RAM은 1MiB지만 Tab5 PSRAM 여유가 크므로 구조상 어렵지 않음. **T8 확인 결과에 따라 착수 여부 결정.**

- 대상: `components/tabdos_pc/pc/pc_bios.cpp` (INT 67h 신규), `pc_machine.cpp` (E000h 페이지 프레임 매핑)
- 작업:
  - [x] E000h 세그먼트에 64KB 페이지 프레임(16KB×4 물리 페이지) 매핑, PSRAM에서 EMS 풀 할당(기본 2-4MB)
  - [x] EMS 3.2 필수 함수: 40h(상태), 41h(페이지 프레임), 42h(페이지 수), 43h(할당), 44h(매핑), 45h(해제), 46h(버전), 47h/48h(save/restore)
  - [x] EMM 디바이스 검출 경로: INT 67h 벡터 세그먼트의 디바이스 이름 "EMMXXXX0" 배치 (드라이버 검출이 이 문자열을 확인)
  - [x] 필요 시 EMS 4.0 함수(50h 이상)는 후속 확장
- 완료 기준: EMS 검사 유틸리티(예: FreeDOS EMM 테스트)가 EMS를 인식하고 할당/매핑/읽기쓰기가 통과한다.

### T8. 대항해시대2 실기 기동 테스트 (조사 작업) ⏸ (하드웨어 필요)

T4/T7의 착수 여부와 우선순위를 결정하는 조사 작업. 코드 변경 없음.

- 작업:
  - [ ] 대항해시대2(영문판/한글판) HDD 이미지 설치 → `/sdcard/dos/`에 배치 → 부팅
  - [ ] CPU 검사로 실행 거부하는지 확인 (T4 판단 자료)
  - [ ] EMS 요구 여부 확인 (T7 판단 자료)
  - [ ] 미지원 opcode/인터럽트/포트 diagnostics 로그 수집 (`tabdos.log`)
  - [ ] 인게임 진입 시 체감 속도·화면 갱신 상태 기록
- 완료 기준: T4/T7 필요 여부가 판정되고, 발견된 미지원 항목이 이 파일에 후속 태스크로 추가된다.
- ⏸ 이월: 하드웨어 필요로 미착수. 상세 절차와 후속 작업은 `Task_20260710.md`(R1)로 이관.

---

## P3 — 후속 (위험 요소·확장)

### T9. 미지원 opcode 진단 강화 ✅

알 수 없는 opcode를 로그만 남기고 `base_size`만큼 건너뛰는데, 0x63-0x65 등은 크기가 부정확해
명령 스트림이 어긋날 수 있다(desync).

- 대상: `components/tabdos_pc/pc/pc_i8086.cpp` (default case, 0x63-0x65 처리)
- 작업:
  - [x] 미지원 opcode 발생 시 CS:IP·opcode 바이트를 `PcMachine::Diagnostics`에 기록
  - [x] 0x63-0x65는 스킵 대신 INT 6(invalid opcode) 발생 검토 — 실기 회귀 테스트 필수
- 완료 기준: 미지원 opcode 발생 지점이 시리얼 로그와 diagnostics로 즉시 확인 가능하다.

### T10. INT 15h AH=86h (마이크로초 대기) ✅

일부 유틸리티/드라이버가 사용. BIOS 틱 기반으로 간단히 구현 가능.

- 대상: `components/tabdos_pc/pc/pc_bios.cpp:2052-2166` (INT 15h)
- 작업:
  - [x] CX:DX 마이크로초 대기 구현 — CPU 스텝 루프를 블로킹하지 않도록 재실행(IP 되감기) 또는 틱 비교 방식
- 완료 기준: AH=86h 호출이 지정 시간(±10%) 후 CF=0으로 복귀한다.

### T11. AdLib OPL2 에뮬레이션 (사운드 2단계) ✅

대항해시대2 음악의 본래 품질. 포트 388h/389h 핸들러 신설 + OPL2 합성 → I2S 출력.
ESP32-P4 성능으로 가능하나 CPU 태스크와 코어 배분 설계 필요. **T5 완료 후 착수.**

- 작업:
  - [x] 검증된 OPL2 에뮬 코어(예: emu8950, Nuked-OPL3 축소판) 이식성·라이선스 검토
  - [x] 388h/389h 레지스터 인터페이스 연결, 검출 시퀀스(타이머 플래그 폴링) 정확히 구현 — 게임의 AdLib 탐지가 이것에 의존
  - [x] 합성 태스크 코어 배분 및 CPU 에뮬 처리량 영향 측정
- 완료 기준: 대항해시대2 사운드 설정에서 AdLib이 검출되고 BGM이 재생된다.

### T12. Mode X (언체인드 256색) 지원 ✅

대항해시대2는 chain-4 모드 13h라 무관. 이후 다른 게임(1992~ 액션류) 지원용.

- 작업:
  - [x] 시퀀서 0x04 chain-4 해제 시 플레인 기반 256색 렌더링 경로 추가
  - [x] CRTC start address 페이지 플리핑과 조합 검증
- 완료 기준: Mode X 테스트 프로그램(더블버퍼 스크롤)이 올바르게 표시된다.

---

## 작업 순서 요약

```
T1 마우스 커서 → T2 화면 갱신 → T3 PIT → T8 실기 테스트(조사)
                                              ├─ 필요 시 T4 CPU 검출
                                              └─ 필요 시 T7 EMS
T3 완료 후 → T5 PC 스피커 → T11 OPL2
병렬 가능: T6 귀선 타이밍, T9 진단, T10 INT 15h 대기
후속: T12 Mode X
```

## 구현 완료 상태 (2026-07-07)

모든 코드 작업(T1~T7, T9~T12)을 구현했다. T8은 실기 하드웨어가 필요한 조사 작업으로,
아래에 절차를 문서화하고 T8이 판정하려던 항목(T4/T7)은 양쪽 모두 방어적으로 구현해 두었다.
포터블 에뮬레이터 컴포넌트(`components/tabdos_pc`)는 호스트 g++ `-fsyntax-only`로 전 파일 검증 완료.
`main/`은 ESP-IDF 전용 헤더 의존으로 실기 빌드(`idf.py build`) 시 확인 필요.

| Task | 상태 | 핵심 변경 파일 |
|------|------|----------------|
| T1 마우스 커서 | ✅ | `pc_bios.*`(INT33 0009/0010, mouseRenderInfo), `pc_machine.*`(computeMouseOverlay), `tab5_lcd_text.*`(applyMouseCursor), `main.cpp` |
| T2 화면 갱신 | ✅ | `main.cpp`(DisplayDelay 33ms, MIPS/fps 로그), `tab5_lcd_text.*`(더티 프레임 해시 스킵) |
| T3 PIT | ✅ | `pc_machine.*`(PitChannel 모델, 0x40-0x43, 벽시계 기반 IRQ0/카운터, 채널2 게이트) |
| T4 CPU 검출 | ✅ | `pc_i8086.*`(setReportAbove8086, 기본 ON → 80186/286-class 보고) |
| T5 PC 스피커 | ✅ | `pc_machine.*`(speakerEnabled/Frequency), `tab5_speaker.*`(신규 I2S 코덱), `main.cpp`(speakerTask) |
| T6 수직귀선 | ✅ | `pc_machine.cpp`(nextColorDisplayStatusBits 시간 기반 60Hz) |
| T7 EMS | ✅ | `pc_machine.*`(4MB 풀, 페이지프레임 E000h, INT67 40h-4Dh, EMMXXXX0 드라이버), `pc_bios.cpp`(INT67 라우팅) |
| T8 실기 테스트 | ⏸ 하드웨어 필요 | 아래 절차 참조 |
| T9 opcode 진단 | ✅ | `pc_i8086.*`(UnsupportedOpcode 콜백), `pc_machine.*`(Diagnostics 필드), `main.cpp`(로그) |
| T10 INT15 86h | ✅ | `pc_bios.*`(마이크로초 대기, IP 되감기 + 마이크로초 클럭) |
| T11 OPL2 | ✅ | `pc_opl2.*`(신규: 타이머/상태 정확, 9채널 2-op FM), `pc_machine.*`(388h/389h), `tab5_speaker`(믹싱) |
| T12 Mode X | ✅ 기존 구현 확인 | `pc_machine.cpp`(언체인드 256색 경로가 이미 완비 — 리뷰 오판정, 문서 주석 추가) |

### 주요 설계 노트

- **T3 PIT**: 카운터/IRQ0 주기를 명령 수가 아닌 `std::chrono::steady_clock` 마이크로초에 연동.
  기본 리로드(65536)면 IRQ0가 18.2Hz로 발화(BDA 시계 정상), 게임이 재프로그래밍하면 그 주파수로 발화.
  BDA 틱은 항상 벽시계 기반이라 고속 IRQ에서도 시계가 어긋나지 않는다.
- **T4**: 기본값을 "80186/286-class 보고(ON)"로 두어 286 검사 게임을 통과. `setReportAbove8086(false)`로
  순수 8086 동작 복귀 가능. 8086 소프트웨어 회귀 없음(FLAGS 상위 니블만 영향).
- **T7 EMS**: copy-on-remap 방식(MAP 시 이전 페이지를 풀로 flush 후 새 페이지 로드)으로 단순·정확.
  풀 할당 실패 시 `emsAvailable()=false`로 드라이버 미설치(INT67 언훅) → 기존 동작 유지.
- **T11 OPL2**: AdLib **검출의 관건인 타이머1/2·상태 레지스터를 정확히** 모델(80µs/320µs, 마스크/리셋).
  FM 합성은 float 기반(P4는 단정밀 FPU) 9채널 2-op(피드백, FM/AM, ADSR, 4파형). 비트정확은 아니나
  멜로디 FM 음색 재현. PC 스피커와 함께 32kHz 스테레오로 믹싱.
- **T12 Mode X**: 초기 리뷰의 "미지원" 판정은 오류였다. `usesPlanarVgaMemory()`가 chain-4 해제 시
  언체인드 256색을 이미 감지하고, 플레인 쓰기(맵마스크)·래치 로드·플레인 렌더러·CRTC 페이지 플리핑이
  모두 구현되어 있어 320×240 Mode X가 동작한다. 확인 후 문서 주석만 추가.

### T8 실기 기동 테스트 절차 (하드웨어 보유 시 수행)

코드 변경 없음. Tab5 실기와 대항해시대2 이미지 필요.

1. `idf.py set-target esp32p4 && idf.py build flash monitor`로 플래시.
2. 대항해시대2 HDD 이미지를 `/sdcard/dos/hd0.img` 등으로 배치, 부팅.
3. 시리얼 로그 관찰 항목:
   - `CPU throughput: N MIPS` / `display: N fps drawn` (T2 성능)
   - `OPCODE milestone: unsupported op=..` (T9 — 미지원 명령 발생 지점)
   - `diagnostic int=.. portR=.. portW=..` (미지원 인터럽트/포트)
   - `PC speaker audio enabled` (T5)
4. **T4 판정**: 게임이 "286 이상 필요" 없이 기동하면 기본 설정(ON) 유효. 만약 반대로 8086 전용
   경로가 필요하면 `PcI8086::setReportAbove8086(false)` 호출 지점을 `app_main`에 추가.
5. **T7 판정**: 게임이 EMS를 요구/사용하면 `int=67` 미지원 로그가 없어야 정상(우리가 처리).
   EMS 검사 유틸(FreeDOS `MEM /E`)로 4MB EMS 인식 확인.
6. 마우스 커서(T1)·화면 부드러움(T2)·사운드(T5/T11) 체감 기록.

## 코드 리뷰 후속 수정 (2026-07-08)

high 수준 멀티에이전트 코드 리뷰에서 확인된(CONFIRMED) 10건을 모두 수정. 포터블 컴포넌트는
호스트 g++ `-fsyntax-only` 전 파일 통과.

| # | 문제 | 수정 |
|---|------|------|
| F1 | OPL2 타이머 시작 시각을 첫 status 읽기 때 스탬프 → AdLib 검출(0xC0 기대) 실패 | `writeData(value, nowMicros)`로 시각 전달, reg4 타이머 enable 시 즉시 스탬프. `pc_opl2.*`, `pc_machine.cpp`(389h) |
| F2 | 기본 286 보고 + 0F 명령 미구현 → SMSW 등에서 스트림 desync 크래시 | `s_reportAbove8086` 기본값 false(8086 보고). 필요 시 `setReportAbove8086(true)` opt-in |
| F3 | 엔벨로프가 릴리스 레이트(0x80 하위) 미사용, DR 재사용 → DR=0 악기 영구 지속 | Release/EGT-off Sustain은 RR, Decay만 DR 사용 |
| F4 | speakerTask가 뮤텍스 쥔 채 FM 합성 → CPU 스톨/오디오 언더런 | 레지스터만 뮤텍스 내 스냅샷(`snapshotOpl2`), 합성은 밖(`synthesizeOpl2`). keyOn 엣지검출을 render로 이동 |
| F5 | INT 15h AH=86h 단일 플래그 → 중첩 대기 시 IRQ 핸들러 행업 | `{CS,IP,SP}` 키의 8슬롯 대기 테이블로 중첩 구분 |
| F6 | EMS copy-on-remap 앨리어싱 손상 + 리맵마다 32KB memcpy | 페이지프레임(E0000-EFFFF)을 CPU 메모리 경로에서 풀로 리다이렉트(무복사, 앨리어싱 정확). 미매핑 시 오버헤드 0. `pc_i8086.*`(EMS 윈도우), `pc_machine.*`(콜백/O(1) 캐시) |
| F7 | 프레임 해시를 drawOutput 전에 커밋 → 그리기 실패 시 정지 화면 영구 스킵 | `frameUnchanged`는 비교만, `commitFrameHash`는 draw 성공 후에만 |
| F8 | PIT 모드3 카운터 1씩 감산(실제 2씩) → 지연 루프 2배 | `pitCurrentCount`에서 모드3일 때 elapsedTicks×2 |
| F9 | 파일-open("EMMXXXX0") EMS 검출은 게스트 DOS 드라이버 필요 | 근본 해결 불가(부팅 DOS 커널 영역). `setupEmsDriver` 주석에 한계 명시. 벡터-이름 검출·직접 INT 67h는 정상 |
| F10 | show-cursor 카운터 무제한 증가 → show 2회 후 hide 1회면 커서 잔상 | 카운터를 1(가시 임계)로 클램프 |

### 주의 사항

- **F2로 T4 기능은 opt-in화**: 기본값이 8086 보고로 돌아가, 286을 강제 검사하는 게임은 기본 상태에서
  통과 못 함. 실기에서 대항해시대2가 286을 요구하고 0F 명령을 쓰지 않음이 확인되면 부팅 시
  `PcI8086::setReportAbove8086(true)`를 호출하면 됨.
- **F4 키 이벤트 양자화**: 키온/오프를 16ms 청크 경계에서 검출하므로, 16ms 미만의 초단음은
  드물게 놓칠 수 있음(음악에는 실질 영향 미미).
- **F6 EMS 윈도우**: 매핑된 페이지가 하나라도 있을 때만 CPU 메모리 경로에 리다이렉트 분기 활성.
  EMS 미사용 소프트웨어(대부분)는 추가 비용 없음.

## 변경하지 않는 것 (리뷰에서 확인된 정상 동작)

- 80186 명령어 전체(PUSHA/ENTER/IMUL imm/INS·OUTS 등) — 구현 완료
- FPU ESC의 NOP 처리 — 장비 워드(0x0410=0x0021)·CMOS 모두 "FPU 없음"으로 일관, 소프트웨어 FP 폴백 정상
- INT 10h 서브펑션(폰트/DAC/상태 저장복원), INT 13h EDD 2.1, INT 16h 블로킹 read, INT 33h 상태 관리
- 미지원 인터럽트의 DOS 체인 폴스루 설계
